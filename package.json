{
  "name": "hellocucumber",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rimraf build && tsc && yarn run cucumber-check",
    "cucumber-check": "cucumber-js --dry-run -p checkParams",
    "cucumber": "cucumber-js",
    "test-all": "yarn run cucumber",
    "test-only": "yarn run cucumber --tags @only --format cucumber-console-formatter",
    "test": "yarn run test-all"
  },
  "engines": {
    "node": ">=14"
  },
  "keywords": [],
  "license": "ISC",
  "author": "levin-sa",
  "dependencies": {
    "@cucumber/cucumber": "^8.0.0-rc.1",
    "@cucumber/html-formatter": "^17.0.0",
    "cucumber-console-formatter": "^1.0.0",
    "rimraf": "^3.0.2",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.4"
  },
  "devDependencies": {
    "@types/node": "^17.0.5",
    "@types/selenium-webdriver": "*"
  }
}
